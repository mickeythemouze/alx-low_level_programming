#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

/*prints all single dihit numbers in base 10*/
int main(void)
{
	int k;

	for (k = 0; k <= 9; k++)
	{
		printf("%d ", k);
	}
	printf("\n");
		return (0);
}
